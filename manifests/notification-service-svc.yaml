---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  labels:
    name: notification-service
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
  selector:
    name: notification
