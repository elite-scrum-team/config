---
apiVersion: v1
kind: Service
metadata:
  name: forwarding-service
  labels:
    name: forwarding-service
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
  selector:
    name: forwarding
