apiVersion: v1
kind: Service
metadata:
  name: event-service
  labels:
    name: event-service
  annotations:
    cloud.google.com/load-balancer-type: "Internal"
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
  selector:
    name: event
